export enum TimesheetTimes {
    //This type represents a timesheet with started_at and ended_at fields
    TIMESHEET_TYPE_DAY_WITH_TIMES = 'day_with_times',
    //This type represent a timesheet (that displays only one day) with only the amount of hours
    TIMESHEET_TYPE_HOURS_PER_DAY = 'hours_per_day',
}
